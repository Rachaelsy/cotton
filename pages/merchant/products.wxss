/* pages/merchant/products.wxss */

.page {
  display: flex;
  flex-direction: column;
  height: 100vh;
  background: #F5F6FA;
}
.scroll { flex: 1; height: 0; }

.header {
  background: linear-gradient(135deg, #B9852F, #C9973A);
  padding: 8rpx 32rpx 28rpx;
}
.header-tag {
  font-size: 20rpx;
  color: rgba(255,255,255,0.65);
  display: block;
  margin-bottom: 12rpx;
  padding-right: 200rpx;
}
.header-row {
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: space-between;
}
.header-title { font-size: 36rpx; font-weight: 700; color: #fff; }
.add-btn {
  display: flex;
  flex-direction: row;
  align-items: center;
  background: rgba(255,255,255,0.25);
  border-radius: 26rpx;
  padding: 10rpx 24rpx;
  gap: 6rpx;
}
.add-icon { font-size: 28rpx; color: #fff; font-weight: 700; }
.add-text { font-size: 24rpx; color: #fff; }

/* Tab */
.tab-bar {
  display: flex;
  flex-direction: row;
  background: #fff;
  border-bottom: 1rpx solid #EAF0E6;
}
.tab-item {
  flex: 1;
  text-align: center;
  padding: 24rpx 0;
  font-size: 26rpx;
  color: #8A948B;
}
.tab-item.active {
  color: #B9852F;
  font-weight: 700;
  border-bottom: 4rpx solid #B9852F;
}

/* Product list */
.prod-list { padding: 20rpx 24rpx; display: flex; flex-direction: column; gap: 16rpx; }
.prod-card {
  background: #fff;
  border-radius: 16rpx;
  padding: 24rpx;
  box-shadow: 0 2rpx 12rpx rgba(33,61,42,0.05);
}
.prod-info { display: flex; flex-direction: row; gap: 20rpx; margin-bottom: 20rpx; }
.prod-icon {
  width: 80rpx; height: 80rpx;
  background: #FFF7ED;
  border-radius: 16rpx;
  display: flex; align-items: center; justify-content: center;
  flex-shrink: 0;
}
.prod-emoji { font-size: 40rpx; }
.prod-detail { flex: 1; }
.prod-name { font-size: 28rpx; font-weight: 600; color: #18231A; display: block; }
.prod-cat { font-size: 22rpx; color: #B7C0B8; margin-top: 4rpx; display: block; }
.prod-meta { display: flex; flex-direction: row; align-items: baseline; gap: 8rpx; margin-top: 8rpx; }
.prod-price { font-size: 30rpx; font-weight: 700; color: #B9852F; }
.prod-unit { font-size: 20rpx; color: #B7C0B8; }
.prod-stock { font-size: 20rpx; color: #2F8A52; margin-left: 8rpx; }
.prod-stock.out { color: #D6453D; }

.prod-actions { display: flex; flex-direction: row; gap: 12rpx; }
.action-btn {
  flex: 1;
  text-align: center;
  padding: 12rpx 0;
  border-radius: 10rpx;
  font-size: 24rpx;
}
.action-btn.edit    { background: #EFF6FF; color: #2563EB; }
.action-btn.off-btn { background: #FFF7ED; color: #D97706; }
.action-btn.on-btn  { background: #F0FDF4; color: #2F8A52; }
.action-btn.del     { background: #FEF2F2; color: #C93D35; }

/* Empty */
.empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 80rpx 0;
  gap: 16rpx;
}
.empty-icon { font-size: 80rpx; }
.empty-text { font-size: 26rpx; color: #B7C0B8; }

/* Modal */
.modal-mask {
  position: fixed;
  top: 0; left: 0; right: 0; bottom: 0;
  background: rgba(0,0,0,0.5);
  z-index: 100;
}
.modal {
  position: fixed;
  left: 0; right: 0; bottom: 0;
  background: #fff;
  border-radius: 28rpx 28rpx 0 0;
  padding: 40rpx 32rpx;
  z-index: 101;
}
.modal-title { font-size: 32rpx; font-weight: 700; color: #18231A; display: block; margin-bottom: 32rpx; }
.form-item { margin-bottom: 24rpx; }
.form-row { display: flex; flex-direction: row; gap: 20rpx; }
.form-item.half { flex: 1; }
.form-label { font-size: 24rpx; color: #58655B; display: block; margin-bottom: 10rpx; }
.form-input {
  width: 100%;
  height: 80rpx;
  background: #F5F6FA;
  border-radius: 12rpx;
  padding: 0 20rpx;
  font-size: 26rpx;
  color: #18231A;
  box-sizing: border-box;
}
.modal-btns { display: flex; flex-direction: row; gap: 20rpx; margin-top: 16rpx; }
.modal-btn {
  flex: 1;
  height: 88rpx;
  border-radius: 16rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 28rpx;
  font-weight: 600;
}
.modal-btn.cancel  { background: #F5F6FA; color: #687468; }
.modal-btn.confirm { background: #B9852F; color: #fff; }
