/* pages/profile/index.wxss */

.page {
  display: flex;
  flex-direction: column;
  min-height: 100vh;
  background: #F5EEE6;
}

/* ===== 导航 ===== */
.pf-nav {
  background: #ffffff;
  display: flex;
  flex-direction: row;
  align-items: center;
  padding-left: 16rpx;
  padding-right: 24rpx;
  padding-bottom: 16rpx;
  box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.06);
}

.nav-back-btn {
  width: 64rpx;
  height: 64rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.back-icon {
  font-size: 52rpx;
  color: #333333;
  font-weight: 300;
  line-height: 1;
}

.pf-title {
  flex: 1;
  text-align: center;
  font-size: 32rpx;
  font-weight: 700;
  color: #1A1A1A;
}

/* ===== 滚动区 ===== */
.pf-scroll {
  flex: 1;
}

/* ===== 头像区 ===== */
.avatar-section {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding-top: 48rpx;
  padding-bottom: 32rpx;
}

.avatar-circle {
  width: 144rpx;
  height: 144rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, #15803d, #22c55e);
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 16rpx;
  box-shadow: 0 8rpx 28rpx rgba(21,128,61,0.30);
}

.avatar-text {
  font-size: 64rpx;
  font-weight: 800;
  color: #ffffff;
  line-height: 1;
}

.avatar-hint {
  font-size: 24rpx;
  color: #AAAAAA;
}

/* ===== 表单卡片 ===== */
.form-card {
  background: #ffffff;
  border-radius: 24rpx;
  margin: 0 28rpx 32rpx;
  padding: 0 32rpx;
  box-shadow: 0 4rpx 20rpx rgba(0,0,0,0.06);
}

.field-row {
  display: flex;
  flex-direction: row;
  align-items: center;
  min-height: 96rpx;
  padding: 20rpx 0;
}

.field-label {
  font-size: 28rpx;
  color: #555555;
  width: 160rpx;
  flex-shrink: 0;
}

.field-input {
  flex: 1;
  font-size: 28rpx;
  color: #1A1A1A;
  text-align: right;
}

.field-ph {
  color: #CCCCCC;
}

.field-readonly {
  flex: 1;
  font-size: 28rpx;
  color: #AAAAAA;
  text-align: right;
}

.field-input-wrap {
  flex: 1;
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: flex-end;
  gap: 8rpx;
}

.field-unit {
  font-size: 26rpx;
  color: #888888;
  flex-shrink: 0;
}

.field-picker {
  display: flex;
  flex-direction: row;
  align-items: center;
  gap: 8rpx;
}

.field-picker-val {
  font-size: 28rpx;
  color: #1A1A1A;
}

.field-picker-arr {
  font-size: 32rpx;
  color: #CCCCCC;
}

.field-div {
  height: 1rpx;
  background: #F5EEE6;
}

/* ===== 保存按钮 ===== */
.save-btn {
  background: linear-gradient(135deg, #15803d, #22c55e);
  border-radius: 999rpx;
  margin: 0 28rpx;
  padding: 32rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 6rpx 20rpx rgba(21,128,61,0.30);
}

.save-btn-off {
  opacity: 0.6;
}

.save-btn-text {
  font-size: 32rpx;
  font-weight: 800;
  color: #ffffff;
}
